[<-- Back to Legend](../legend.md)

# Command Name: Teleport
Keyword: teleport

### Usages
```
teleport <x> <y> <z>
```

### Regex
```regexp
(((teleport)( ([\^~]?-?\d*(\.\d*)?))( ([\^~]?-?\d*(\.\d*)?))( ([\^~]?-?\d*(\.\d*)?))))
```

### Raw Documentation
```yml
# teleport <x> <y> <z>
```
