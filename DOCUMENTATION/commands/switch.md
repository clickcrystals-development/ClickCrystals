[<-- Back to Legend](../legend.md)

# Command Name: Switch
Keyword: switch

### Usages
```
switch <identifier>
switch <int>
switch back
```

### Regex
```regexp
(((switch)( (back)))|((switch)( (-?\d+)))|((switch)( !?(([#:](\w+)(\[(.*)\])?,?)+))))
```

### Raw Documentation
```yml
# switch back
# switch <int>
# switch <identifier>
```
